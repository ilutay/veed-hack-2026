# Secrets — never commit. Only the template is tracked.
.env
.env.*
!.env.example

# Run artifacts (generated media, provider responses)
artifacts/

# App
/node_modules
/.next/
/dist
*.tsbuildinfo
__pycache__/
*.pyc

# OS / editor noise
.DS_Store
*.swp
