# Secrets — never commit. Only the template is tracked.
.env
.env.*
!.env.example

# Run artifacts (generated media, provider responses)
artifacts/

# OS / editor noise
.DS_Store
*.swp
.gstack/

# Local Next.js application state
node_modules/
.next/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
